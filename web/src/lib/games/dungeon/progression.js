function clamp(value,min,max){return Math.max(min,Math.min(max,value))}
function pickRandom(items,random){if(!items.length)return null;return items[Math.min(items.length-1,Math.floor(random()*items.length))]}
export function createChapterPlan(chapter=1,random=Math.random){const length=4+Math.floor(random()*5),plan=Array.from({length},()=> 'combat');plan[length-1]='boss';const eliteTarget=Math.min(2,Math.floor((length-2)/3)+(chapter>=4?1:0)),eliteCandidates=Array.from({length:Math.max(0,length-2)},(_,index)=>index+1);for(let i=0;i<eliteTarget&&eliteCandidates.length;i++){const chosen=pickRandom(eliteCandidates,random);if(chosen==null)break;plan[chosen]='elite';eliteCandidates.splice(eliteCandidates.indexOf(chosen),1)}const restCandidates=[];for(let i=1;i<length-2;i++)if(plan[i]==='combat')restCandidates.push(i);if(random()<.65&&restCandidates.length){const i=pickRandom(restCandidates,random);if(i!=null)plan[i]='rest'}const treasure=[];for(let i=1;i<length-2;i++)if(plan[i]==='combat')treasure.push(i);if(treasure.length&&random()<.55){const i=pickRandom(treasure,random);if(i!=null)plan[i]='treasure'}if(length>=4)plan[length-2]='antechamber';return plan}
export const roomRoleAt=p=>p?.chapterPlan?.[Math.max(0,(p.chapterFloor??1)-1)]??'combat'
export function createRunProgress(random=Math.random){const chapterPlan=createChapterPlan(1,random);return{floor:1,chapter:1,chapterFloor:1,chapterLength:chapterPlan.length,chapterPlan,roomRole:chapterPlan[0]}}
export function advanceProgress(p,random=Math.random){if((p.chapterFloor??1)>=(p.chapterPlan?.length??p.chapterLength??1)){const chapter=(p.chapter??1)+1,chapterPlan=createChapterPlan(chapter,random);return{floor:(p.floor??1)+1,chapter,chapterFloor:1,chapterLength:chapterPlan.length,chapterPlan,roomRole:chapterPlan[0]}}const next={...p,floor:(p.floor??1)+1,chapterFloor:(p.chapterFloor??1)+1};next.roomRole=roomRoleAt(next);return next}
export function difficultyProfile({floor=1,chapter=1,roomRole='combat'}={}){const depth=Math.max(0,floor-1),cd=Math.max(0,chapter-1),roleHp=roomRole==='elite'?1.28:roomRole==='boss'?1.7:roomRole==='antechamber'?0.88:1;const roleDamage=roomRole==='elite'?1.2:roomRole==='boss'?1.35:1,roleWave=roomRole==='elite'?2:roomRole==='boss'?-4:roomRole==='antechamber'?-2:0;return{hpMultiplier:(1+depth*.075+cd*.04)*roleHp,damageMultiplier:(1+depth*.042+cd*.025)*roleDamage,speedMultiplier:clamp(1+depth*.006,1,1.45),waveCount:clamp(5+Math.floor(depth/3)+roleWave,roomRole==='boss'?1:3,14),eliteCount:roomRole==='elite'?clamp(2+Math.floor(chapter/3),2,5):clamp(Math.floor(chapter/5),0,2),bossHpMultiplier:clamp(1+cd*.24,1,4.6),bossDamageMultiplier:clamp(1+cd*.12,1,2.8),bossCooldownMultiplier:clamp(1-cd*.035,.58,1)}}
