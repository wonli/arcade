const PROFILES={skeleton:{mode:'pressure',closeRange:76,strafeMultiplier:.48},fast:{mode:'dash',triggerRange:190,dashSpeedMultiplier:2.35,dashMs:280,cooldownMs:1450},brute:{mode:'windup',triggerRange:108,slamRadius:82,windupMs:420,cooldownMs:2200},ranged:{mode:'kite',strafeMultiplier:.34}}
const INTENTS={skeleton:{leadMs:0,scaleX:1,scaleY:1},fast:{leadMs:180,scaleX:1.12,scaleY:.82},brute:{leadMs:420,scaleX:.92,scaleY:1.12},ranged:{leadMs:160,scaleX:1.05,scaleY:.95,flash:true}}
export const enemyBehaviorProfile=(archetype='skeleton')=>PROFILES[archetype]??PROFILES.skeleton
export const enemyIntentProfile=(archetype='skeleton')=>INTENTS[archetype]??INTENTS.skeleton
export function enemyBehaviorStep(enemy,{distance=Infinity,time=0}={}){const p=enemyBehaviorProfile(enemy?.archetype);if(enemy?.archetype==='fast'&&time>=(enemy.nextSpecialAt??Infinity)&&distance<=p.triggerRange)return{action:'dash',durationMs:p.dashMs,speedMultiplier:p.dashSpeedMultiplier,cooldownMs:p.cooldownMs};if(enemy?.archetype==='brute'&&time>=(enemy.nextSpecialAt??Infinity)&&distance<=p.triggerRange)return{action:'slam-windup',durationMs:p.windupMs,radius:p.slamRadius,cooldownMs:p.cooldownMs};if(enemy?.archetype==='skeleton'&&distance<=p.closeRange)return{action:'sidestep',speedMultiplier:p.strafeMultiplier,sign:enemy?.strafeSign??1};return{action:'approach'}}
